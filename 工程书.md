# 阅 · 读书助手 — 工程书

## 项目简介

**阅 · 读书助手** 是一款纯前端的读书辅助网页，主要面向**阅读实体书**的场景：手边放着纸质书，用手机快速记录章节、摘抄、疑问与感想。

所有数据保存在浏览器 `localStorage` 中，不上传服务器，无需注册登录。适合部署到 GitHub + Vercel，在手机上当作轻量 Web App 使用（可「添加到主屏幕」）。

| 项目属性 | 说明 |
|---------|------|
| 类型 | 纯静态单页应用（SPA） |
| 技术栈 | HTML + CSS + 原生 JavaScript |
| 构建工具 | 无（无需编译） |
| 数据存储 | 浏览器 localStorage |
| 目标平台 | 移动端优先，兼容桌面浏览器 |

---

## 功能清单

### 书架

- 添加书籍（实体书 / 电子书两种类型）
- 书籍列表展示：书名、类型标签、笔记数或阅读进度
- 点击书籍进入对应界面（实体书 → 笔记页，电子书 → 阅读页）
- 长按或 Shift+点击：打开操作菜单（打开、查看笔记、从头阅读、删除）
- 首次使用自动注入示例实体书，便于体验

### 实体书 · 读书笔记

- 按书籍管理多条笔记
- 笔记列表自上而下排列（旧在上、新在下），顺序持久保存
- 左侧手柄可拖动调整笔记顺序
- 笔记列表折叠展示：章节为标题，首句为预览，点击展开全文
- 展开后可查看笔记内容、疑问、感想，并进入编辑
- 右下角浮动按钮快速新建笔记
- 独立全屏编辑页，字段：章节、笔记内容、疑问、感想
- 支持编辑、删除单条笔记

### 电子书 · 在线阅读（附加能力）

- 粘贴文字或导入 `.txt` / `.md` 文件
- 滚动 / 分页两种阅读模式
- 阅读进度自动保存
- 字号、行距、页边距、字体（宋体 / 黑体）可调
- 阅读界面可跳转笔记页，边读边记

### 外观设置

- 三种主题：浅色、护眼、深色
- 书架、笔记页、电子书阅读页均可打开设置（⚙）
- 切换主题即时生效并持久保存

---

## 目录结构

```
阅读器/
├── index.html          # 页面结构：各视图区块 + 弹窗
├── css/
│   └── style.css       # 样式、主题变量、响应式布局
├── js/
│   └── app.js          # 全部业务逻辑（单文件）
├── vercel.json         # Vercel 静态站点部署配置
└── 工程书.md           # 本文件
```

---

## 页面结构（视图）

应用通过 `#app[data-view]` 切换视图，同一 HTML 内包含多个 `<section>`，用 `.hidden` 控制显示。

| 视图 ID | 说明 | 主要入口 |
|--------|------|---------|
| `view-shelf` | 书架 | 默认首页 |
| `view-notes` | 某书的笔记列表 | 点击实体书 |
| `view-note-edit` | 新建 / 编辑笔记 | 笔记页 ✎ 或「编辑」 |
| `view-reader` | 电子书阅读 | 点击电子书 |

弹窗（`<dialog>`）：

| 弹窗 | 用途 |
|-----|------|
| `dialog-add` | 添加书籍 |
| `dialog-settings` | 外观 / 阅读设置 |
| `dialog-book-actions` | 书籍操作菜单 |

---

## 代码结构（app.js）

单文件 IIFE，按职责分块：

```
app.js
├── 常量与状态          STORAGE_*、settings、books、currentBookId 等
├── Storage             load/save settings & books，migrateBooks
├── 工具函数            格式化日期、escapeHtml、文本处理等
├── 主题与设置          applySettings、syncSettingsUI、openSettingsDialog
├── 导航                showView、openBookEntry
├── 书架                renderShelf、addBook、openBookActions
├── 笔记                renderNotesView、openNoteEditor、saveNote、deleteNote
├── 电子书阅读          openReader、分页、进度保存
└── 事件绑定与初始化    bindEvents、init
```

### 关键函数

| 函数 | 作用 |
|-----|------|
| `showView(name)` | 切换 shelf / notes / note-edit / reader |
| `openBookEntry(id)` | 根据书籍类型跳转笔记或阅读 |
| `openNoteEditor(noteId?)` | 打开笔记编辑页，`null` 为新建 |
| `applySettings()` | 应用主题到 `html[data-theme]`，更新阅读区样式 |
| `migrateBooks()` | 加载旧数据时补全字段，兼容升级 |

---

## 数据模型

### localStorage 键

| 键名 | 内容 |
|-----|------|
| `yue-books-v1` | 书籍数组 JSON |
| `yue-settings-v1` | 用户设置 JSON |
| `yue-demo-seeded-v3` | 是否已注入示例书（标记位） |

### 书籍 Book

```js
{
  id: string,           // 唯一 ID
  title: string,        // 书名
  author: string,       // 作者（实体书，选填）
  content: string,      // 电子书正文；实体书为空字符串
  type: "physical" | "digital",
  notes: Note[],        // 笔记列表
  progress: number,     // 电子书阅读进度 0–100
  updatedAt: number     // 最后更新时间戳
}
```

### 笔记 Note

```js
{
  id: string,
  chapter: string,      // 章节（列表标题优先使用）
  content: string,      // 笔记内容 / 摘抄
  question: string,     // 疑问
  reflection: string,   // 感想
  order: number,        // 排序序号，越小越靠上
  createdAt: number,
  updatedAt: number
}
```

默认按 `order` 升序；新建笔记 `order` 为当前最大值 + 1（追加在底部）。编辑内容不会改变顺序，仅拖拽会更新 `order`。

### 设置 Settings

```js
{
  theme: "light" | "sepia" | "dark",
  fontSize: number,     // 14–28，电子书用
  lineHeight: number,
  margin: number,
  mode: "scroll" | "page",
  font: "serif" | "sans"
}
```

保存笔记时，「笔记内容」「疑问」「感想」至少填一项。

---

## 样式与主题

- CSS 变量定义在 `:root` 与 `html[data-theme="…"]` 上
- 默认浅色写在 `:root`，JS 启动时同步 `data-theme`
- 移动端适配：`viewport-fit=cover`、安全区 `env(safe-area-inset-*)`
- 最大内容宽度 720px，大屏居中

---

## 本地运行

```powershell
cd 阅读器
npx serve .
```

浏览器打开终端输出的地址；手机与电脑同一 WiFi 下，可用局域网 IP 访问测试。

---

## 部署（GitHub + Vercel）

1. 将仓库推送到 GitHub
2. 在 [vercel.com](https://vercel.com) Import 该仓库
3. 框架选 Other / 静态站点，根目录即项目根
4. Deploy 完成后用手机访问生成的 URL

`vercel.json` 已配置 SPA 回退到 `index.html`。

---

## 版本与变更记录

| 阶段 | 内容 |
|-----|------|
| v0.1 | 纯前端读书页，电子书导入与阅读 |
| v0.2 | 实体书模式、笔记 CRUD、localStorage |
| v0.3 | 笔记折叠列表、独立编辑页（章节 / 内容 / 疑问 / 感想） |
| v0.4 | 移除页码；书架与笔记页增加外观设置；主题 CSS 修复 |

---

## 后续可扩展方向

- 笔记导出（Markdown / 文本）
- 全文搜索笔记
- 数据导入 / 导出备份
- EPUB 支持
- PWA 离线缓存（Service Worker）

---

## 维护说明

- 修改数据结构时，在 `migrateBooks` / `migrateNote` 中做兼容，并视情况升级 `STORAGE_*` 版本号
- 新增视图：在 `index.html` 增加 `<section>`，在 `showView` 与 `bindEvents` 中注册
- 样式尽量沿用现有 CSS 变量与 `.btn`、`.field` 等组件类，保持视觉一致
