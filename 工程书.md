# 阅 · 读书助手 — 工程书

## 项目简介

**阅 · 读书助手** 是一款纯前端的读书辅助网页，主要面向**阅读实体书**的场景：手边放着纸质书，用手机快速记录章节、摘抄、疑问与感想。

所有数据保存在浏览器 `localStorage` 中，不上传服务器，无需注册登录。适合部署到 GitHub + Vercel，在手机上当作轻量 Web App 使用（可「添加到主屏幕」）。

| 项目属性 | 说明 |
|---------|------|
| 类型 | 纯静态单页应用（SPA） |
| 技术栈 | HTML + CSS + 原生 JavaScript |
| 构建工具 | 无（无需编译） |
| 数据存储 | 浏览器 localStorage |
| 目标平台 | 移动端优先，兼容桌面浏览器 |

---

## 功能清单

### 书架

- 添加书籍（**实体书** / **电子书**）
  - 实体书：书名（必填）、作者（选填），无需导入正文
  - 电子书：粘贴文字或导入 `.txt` / `.md`
- 列表展示：书名、类型标签、笔记条数或阅读进度
- 点击书籍：实体书 → 笔记列表；电子书 → 阅读页
- **Shift+点击** 或 **右键**：书籍操作菜单（打开、查看笔记、从头阅读、删除）
- 右上角 **⚙**：外观设置（全站可用）
- 首次使用注入示例实体书（含两条示例笔记）

### 实体书 · 读书笔记

**列表与排序**

- 笔记**自上而下**排列：越靠上越早，新建默认在**最下方**
- 顺序由 `order` 字段持久化；编辑内容**不改变**位置
- **长按**左侧 ⠿ 手柄进入拖拽（手机约 0.42s，手柄有脉冲提示）
- **鼠标**：按住手柄即可拖动
- 拖拽时：条目浮起、虚线占位、其余条目 FLIP 动画让位、松手后滑入落位

**列表展示（折叠）**

- 标题：章节名，无章节则显示「未分章」
- 预览：笔记内容 / 疑问 / 感想 的首句（灰色小字）
- 点击条目：展开全文（分块显示内容、疑问、感想）
- 展开后点 **编辑** 进入编辑页

**新建与编辑**

- 右下角 **✎** 浮动按钮：新建笔记
- 独立全屏页 `view-note-edit`，字段：
  - 章节
  - 笔记内容（摘抄）
  - 疑问
  - 感想
- 保存时「内容 / 疑问 / 感想」至少填一项
- 编辑模式可删除笔记；**无页码字段**

### 电子书 · 在线阅读（附加）

- 滚动 / 分页阅读，进度自动保存
- 字号、行距、页边距、字体可调
- 工具栏 **✎** 跳转该书的笔记列表（与实体书共用笔记模型）

### 外观设置

- 主题：浅色 / 护眼 / 深色
- 入口：书架 ⚙、笔记页 ⚙、电子书阅读 ⚙
- 切换后立即生效并写入 `localStorage`
- 默认主题变量写在 CSS `:root`，避免未加载 JS 时无样式

---

## 目录结构

```
阅读器/
├── index.html          # 页面结构：4 个视图 + 3 个弹窗
├── css/
│   └── style.css       # 样式、主题、笔记拖拽动画
├── js/
│   └── app.js          # 全部业务逻辑（单文件 IIFE）
├── vercel.json         # Vercel 静态部署 / SPA 回退
└── 工程书.md           # 本文件
```

---

## 页面结构（视图）

通过 `#app[data-view]` 与 `.hidden` 切换，无路由库。

| `data-view` | 区块 ID | 说明 |
|-------------|---------|------|
| `shelf` | `view-shelf` | 书架（默认） |
| `notes` | `view-notes` | 某书的笔记列表 |
| `note-edit` | `view-note-edit` | 新建 / 编辑笔记 |
| `reader` | `view-reader` | 电子书阅读 |

### 弹窗（`<dialog>`）

| ID | 用途 |
|----|------|
| `dialog-add` | 添加书籍（实体书 / 电子书切换） |
| `dialog-settings` | 外观与电子书阅读设置 |
| `dialog-book-actions` | 书籍操作菜单 |

### 导航关系（简图）

```
书架 (shelf)
  ├─ 点击实体书 ──→ 笔记列表 (notes)
  │                    ├─ ✎ / 编辑 ──→ 笔记编辑 (note-edit) ──→ 返回 notes
  │                    └─ ← 返回书架
  └─ 点击电子书 ──→ 阅读 (reader)
                       ├─ ✎ ──→ 笔记列表 (notes) ──→ ← 可回到 reader
                       └─ ← 返回书架
```

---

## 代码结构（app.js）

单文件立即执行函数（IIFE），`"use strict"`。

```
app.js
├── 常量与全局状态
│     STORAGE_*、settings、books、currentBookId、expandedNoteId …
├── Storage
│     load/save、migrateBooks、migrateNote、ensureBookNoteOrders
├── 工具
│     escapeHtml、formatDate、文本分段等
├── 主题与设置
│     applySettings、syncSettingsUI、openSettingsDialog
├── 导航
│     showView、openBookEntry、openNotes、openNoteEditor、closeNoteEditor
├── 书架
│     renderShelf、addBook、openBookActions
├── 笔记列表
│     sortNotes、renderNotesView、toggleNoteExpand、renderNoteBody
├── 笔记拖拽排序
│     bindNoteDragReorder、movePlaceholderWithAnimation、persistNoteOrderFromDom
├── 笔记编辑
│     saveNote、deleteNote
├── 电子书阅读
│     openReader、paginate、进度保存
└── bindEvents / init
```

### 关键函数

| 函数 | 作用 |
|-----|------|
| `showView(name)` | 切换 `shelf` / `notes` / `note-edit` / `reader` |
| `openBookEntry(id)` | 按 `type` 打开笔记页或阅读页 |
| `openNoteEditor(noteId?)` | `null` 新建，否则编辑 |
| `sortNotes(notes)` | 按 `order` 升序，相同时按 `createdAt` |
| `bindNoteDragReorder()` | 绑定长按拖拽（仅初始化一次） |
| `persistNoteOrderFromDom()` | 按 DOM 顺序写回 `order` |
| `applySettings()` | 设置 `html[data-theme]` 与阅读区样式 |
| `migrateBooks()` / `migrateNote()` | 旧数据兼容 |

---

## 笔记拖拽实现要点

> 修改拖拽逻辑时请优先阅读本节。

1. **事件监听在 `document` 上**  
   `setPointerCapture` 之后 `pointermove` 不会冒泡到 `notesList`，必须在 document 层监听并按 `pointerId` 过滤。

2. **触摸 vs 鼠标**  
   - `pointerType === "touch"`：长按 `420ms` 后开始拖拽（`pending` + `is-press-waiting`）  
   - 否则：按下即拖  
   - 长按等待期间移动超过 `12px` 会 `cancelPending()`

3. **拖拽过程**  
   - 原位置插入 `.note-drag-placeholder`  
   - 条目加 `.is-dragging-floating` 挂到 `body`，`position: fixed` 跟手  
   - `body.is-note-dragging` 禁止页面滚动  
   - 换位时对其它条目做 **FLIP** 动画（`movePlaceholderWithAnimation`）

4. **结束**  
   - 条目动画吸附到占位框（`.is-snapping`）  
   - 插回列表，删除占位，调用 `persistNoteOrderFromDom()`

5. **手柄 DOM**  
   使用 `<div class="note-drag-handle">`，不用 `<button>`，减少 iOS 触摸冲突。

### 相关 CSS 类

| 类名 | 含义 |
|-----|------|
| `is-sorting` | 列表处于排序中 |
| `is-press-waiting` | 长按等待中 |
| `is-dragging-floating` | 条目浮起拖动 |
| `is-dragging-active` | 浮起放大态 |
| `is-snapping` | 松手落位动画 |
| `note-drag-placeholder` | 虚线占位条 |
| `is-flipping` | FLIP 位移动画中 |

---

## 数据模型

### localStorage 键

| 键名 | 内容 |
|-----|------|
| `yue-books-v1` | 书籍数组 JSON |
| `yue-settings-v1` | 用户设置 JSON |
| `yue-demo-seeded-v3` | 是否已注入示例书 |

### 书籍 `Book`

```js
{
  id: string,
  title: string,
  author: string,              // 实体书选填
  content: string,             // 电子书正文；实体书为 ""
  type: "physical" | "digital",
  notes: Note[],
  progress: number,            // 电子书 0–100
  updatedAt: number
}
```

### 笔记 `Note`

```js
{
  id: string,
  chapter: string,             // 列表标题优先
  content: string,             // 摘抄 / 笔记内容
  question: string,            // 疑问
  reflection: string,          // 感想
  order: number,               // 越小越靠上
  createdAt: number,
  updatedAt: number
}
```

**排序规则**

- 展示：`order` 升序
- 新建：`order = max(现有 order) + 1`（底部）
- 拖拽：DOM 顺序写回 `0 … n-1`
- 删除：`ensureBookNoteOrders` 重排为连续序号

### 设置 `Settings`

```js
{
  theme: "light" | "sepia" | "dark",
  fontSize: number,            // 14–28，仅电子书
  lineHeight: number,
  margin: number,
  mode: "scroll" | "page",
  font: "serif" | "sans"
}
```

---

## 样式与主题

- 主题变量：`:root`（默认浅色）+ `html[data-theme="light|sepia|dark"]`
- 字体：Google Fonts — Noto Serif SC（阅读）、Source Sans 3（界面）
- 布局：最大宽度 720px；`env(safe-area-inset-*)` 适配刘海屏
- 组件：`.btn`、`.field`、`.chip`、`.dialog`、`.fab`、`.note-card` 等

---

## 本地运行

```powershell
cd 阅读器
npx serve .
```

浏览器打开终端地址；手机与电脑同一 WiFi 时，可用电脑的局域网 IP 访问。

---

## 部署（GitHub + Vercel）

1. 推送仓库到 GitHub  
2. [vercel.com](https://vercel.com) → Import 项目  
3. 框架选 **Other**，根目录为仓库根  
4. 部署完成后用手机打开 URL，可「添加到主屏幕」

`vercel.json` 将未知路径回退到 `index.html`。

---

## 版本与变更记录

| 版本 | 内容 |
|-----|------|
| v0.1 | 纯前端读书页，电子书导入与阅读 |
| v0.2 | 实体书模式、笔记 CRUD、localStorage |
| v0.3 | 笔记折叠列表；独立编辑页（章节 / 内容 / 疑问 / 感想） |
| v0.4 | 移除页码；全站外观设置；主题 CSS 修复（`:root` + `html[data-theme]`） |
| v0.5 | 笔记 `order` 自上而下排序；新建在底部 |
| v0.6 | 拖拽排序动画（浮起、占位、FLIP）；长按拖动（触摸）；document 级指针事件 |

---

## 后续可扩展方向

- 笔记导出（Markdown / 文本）
- 按章节自动分组展示
- 全文搜索笔记
- 数据导入 / 导出备份
- EPUB 支持
- PWA 离线缓存（Service Worker）

---

## 维护说明

1. **数据结构变更**  
   在 `migrateNote` / `migrateBooks` / `ensureBookNoteOrders` 中做兼容；必要时升级 `STORAGE_*` 版本号。

2. **新增视图**  
   在 `index.html` 增加 `<section>`，扩展 `showView()` 与 `bindEvents()`。

3. **拖拽相关**  
   勿把 `pointermove` / `pointerup` 只绑在 `notesList`；触摸设备保留长按逻辑，避免与滚动冲突。

4. **样式**  
   优先复用 CSS 变量与现有组件类，保持视觉一致。
